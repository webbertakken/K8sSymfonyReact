{% extends 'react_base.html.twig' %}

{% set demoScene = demoScene|default('invalid') %}

{% block styles %}
  <link rel="stylesheet" href="{{ asset('scenes/demo/' ~ demoScene ~ '.css') }}" type="text/css" />
{% endblock %}

{% block application %}
  <script src="{{ asset('scenes/demo/' ~ demoScene ~ '.js') }}"></script>
{% endblock %}
